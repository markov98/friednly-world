<main class="dashboard">
    <section id="dashboard">
        {{#if isEmpty}}
            <div id="no-post">
                <h2>There are no animals in need now! </h2>
            </div>
        {{else}}
            <h2>Animals in Need</h2>
            <ul class="card-wrapper">
            {{#each animals}}
                {{>dashboardAnimalTemplate}}
            {{/each}}
        </ul>
        {{/if}}
    </section>
</main>